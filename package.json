{
  "scripts": {
    "start": "node index.js",
    "test": "cd backend && npm install && npx jest --ci --reporters=default --reporters=jest-junit",
    "build": "echo No build step needed"
  },
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/frontend/"
    ]
  }
}