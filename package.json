{
  "scripts": {
    "start": "node index.js",
    "test": "cd backend && npm test",
    "build": "echo No build step needed"
  }
},
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/frontend/"
    ]
  }
}